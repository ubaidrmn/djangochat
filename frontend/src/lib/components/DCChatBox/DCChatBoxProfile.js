import { styled } from "styled-components"
import { DARK_BACKGROUND_COLOR_PRIMARY } from "../constants";

function DCChatBoxProfile(props) {
    
    const Div = styled.div`
        background-color: ${DARK_BACKGROUND_COLOR_PRIMARY};
        max-width: 100%;
        width: 100%;
        height: 100%;
        max-height: 100%;
        border-left: 1px solid rgb(76 96 138);
    `;

    return <Div>

    </Div>
}

export default DCChatBoxProfile;
